import { MobileContractionPipe } from './mobile-contraction-pipe';

describe('MobileContractionPipe', () => {
  it('create an instance', () => {
    const pipe = new MobileContractionPipe();
    expect(pipe).toBeTruthy();
  });
});
